import DefaultPage from "@/components/DefaultPage";

export default function test() {
  return (
    <DefaultPage title="test">
      <p className="">
        This is a project I worked on for a class. It is a web application that
        allows users to create and share their own recipes. It is built using
        the MERN stack (MongoDB, Express, React, Node.js). It also uses the
        Material UI library for styling.
      </p>
    </DefaultPage>
  );
}
